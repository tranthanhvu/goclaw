VITE_BACKEND_PORT=18790
VITE_BACKEND_HOST=localhost
VITE_WS_URL=ws://localhost:18790/ws
# Set both when hosting the SPA on a different origin than the gateway (e.g. Vercel).
# The gateway must list the SPA origin in gateway.allowed_origins.
# VITE_API_URL=https://gw.example.com
# VITE_WS_URL=wss://gw.example.com/ws
